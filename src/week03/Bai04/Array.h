#pragma once

#include <iostream>
#include <vector>

using std::vector;

class Array {
   private:
    int n;
    vector<int> list;

   public:
    Array(/* args */);
    ~Array();
};

Array::Array(/* args */) {
}

Array::~Array() {
}
